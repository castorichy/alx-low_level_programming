#include "main.h"
#include <stdio.h>
/**
 * main - main
 *
 * Return: non
 */
int main(void)
{
	int alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');

	return (0);
}
