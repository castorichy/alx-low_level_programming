#include "main.h"

/**
 * positive_or_negative - checks if the number is positive or negative
 * @i: stores an int
 * Return: NULL
 */
void positive_or_negative(int i)
{
	if (i == 0)
	{
		printf("%d is zero", i);
	}

}
